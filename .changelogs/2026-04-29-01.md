### Implement Local endpoint proxy routing [12:03:28] <udeua6i7tf>

🟢 OpenAI Codex (`gpt-5.5`) [phase4/node-ip-labeler]

**Tags:** #external-traffic-policy #controller #proxy

**User request summary:** Finish the in-progress externalTrafficPolicy Local implementation so the controller passes endpoint information to the proxy and the proxy dials endpoints instead of ClusterIP.

**Implementation summary:**
- Added controller endpoint discovery for Services annotated with bulb.toturi.tech/external-traffic-policy=Local and rendered endpoint hostports into proxy DaemonSet args.
- Added proxy endpoint parsing and round-robin endpoint upstream selection for TCP and UDP forwarding specs.
- Added Endpoints watch and RBAC plus controller/proxy tests covering ready endpoint propagation and endpoint argument parsing.

---

### Refresh contract-tightening project docs [12:18:23] <mdto8kyiuf>

🟢 OpenAI Codex (`gpt-5.5`) [chore/contract-tightening]

**Tags:** #docs #contract-tightening

**User request summary:** Create a feature branch for the whole contract-tightening scope, then refresh CLAUDE.md and README.md with current project knowledge, explicit remaining work, and removal of stale statements.

**Implementation summary:**
- Created the chore/contract-tightening branch from main.
- Updated CLAUDE.md to reflect the current v0.0.5 Phase 4 baseline, current components, deferred DNS provider work, and the pre-Helm contract-tightening backlog.
- Updated README.md to remove stale Phase 1/static ConfigMap instructions and document the current manifest flow, dry-run DNS behavior, remaining contract work, and deferred packaging.

---

### Refresh README manifest requirements [12:23:14] <e5iiz7nmuf>

🟢 OpenAI Codex (`gpt-5.5`) [chore/contract-tightening]

**Tags:** #docs #contract-tightening

**User request summary:** Make README.md more current by removing Phase 1 sanity-check wording and documenting supported firewall backends beyond RHEL.

**Implementation summary:**
- Reframed the manifest section as the current post-Phase-4 raw manifest baseline rather than a Phase 1-style trial flow.
- Updated host requirements to require Linux nodes with firewalld, iptables, or nftables instead of a RHEL-family firewalld-only setup.

**Explicit user constraints:**
- Keep Cloudflare and DNS-provider integration work out of scope.

---

### Clarify README phase status [12:25:28] <y6yh2rdouf>

🟢 OpenAI Codex (`gpt-5.5`) [chore/contract-tightening]

**Tags:** #docs #contract-tightening

**User request summary:** Reread README.md and make it actually up to date, especially around deferred DNS integrations and completedness of the project phases.

**Implementation summary:**
- Made the README status block explicitly state which phases are done, mostly done, or not started.
- Replaced the vague roadmap phase wording with a phase completeness table and concrete remaining work per phase.
- Clarified that DNS provider publishing is intentionally deferred without naming Cloudflare as part of the active scope.

**Explicit user constraints:**
- Ignore Cloudflare and related DNS provider integration work as an implementation scope.

---

### Implement Service conflict conditions [12:34:57] <yijuk9mvuf>

🟢 OpenAI Codex (`gpt-5.5`) [chore/contract-tightening]

**Tags:** #controller #contract-tightening #status

**User request summary:** Implement the next roadmap slice: Service hostPort conflict detection, LBPort owner conflict handling, and Service conditions/events before Helm.

**Implementation summary:**
- Added controller-side conflict detection for duplicate Service ports, overlapping Service node scopes on the same port/protocol, and colliding LBPort ownership.
- Added Service status conditions for Reconciled, PortConflict, InvalidService, and NoReadyEndpoints, clearing stale loadBalancer ingress when reconciliation fails closed.
- Emitted Service events for successful reconciliation, Service port conflicts, LBPort owner conflicts, invalid annotations/specs, and Local policy without ready endpoints.
- Added controller tests for success conditions, conflicts, disjoint node selectors, owner conflicts, invalid annotations, and Local policy without ready endpoints.
- Updated README.md and CLAUDE.md to mark this contract-tightening slice complete and keep the remaining pre-Helm backlog current.

**Explicit user constraints:**
- Keep Cloudflare and DNS-provider integrations out of scope.

---

### Implement proxy health probes [12:42:18] <6e0v3v90uf>

🟢 OpenAI Codex (`gpt-5.5`) [chore/contract-tightening]

**Tags:** #proxy #health #contract-tightening

**User request summary:** Implement the proxy health contract slice: listener liveness, upstream-aware readiness where possible, and clear UDP-only behavior.

**Implementation summary:**
- Added proxy HTTP health server with /healthz and /readyz on the configurable health bind address, defaulting to :8081 from the proxy command.
- Made /readyz dial TCP upstreams with a short timeout, preferring Local endpoint hostports when configured, while UDP-only Services report ready after listeners bind.
- Updated generated proxy DaemonSets to expose a named probes port and use HTTP liveness/readiness probes instead of user-port TCP socket probes.
- Added proxy and DaemonSet tests covering healthy TCP upstreams, unreachable upstream readiness failures, Local endpoint readiness, and UDP-only readiness behavior.
- Updated README.md and CLAUDE.md so proxy health is documented as implemented and removed from the pending pre-Helm backlog.

**Explicit user constraints:**
- Keep Cloudflare and DNS-provider integrations out of scope.

---

### Implement keep-on-uninstall annotation [12:50:16] <qczp04c7uf>

🟢 OpenAI Codex (`gpt-5.5`) [chore/contract-tightening]

**Tags:** #controller #contract-tightening #annotations

**User request summary:** Implement the keep-on-uninstall truthfulness slice so the documented annotation is no longer a fake API surface.

**Implementation summary:**
- Added bulb.toturi.tech/keep-on-uninstall as a controller annotation constant and persisted it onto generated proxy DaemonSets.
- Changed Service delete and type/class cleanup to honor the persisted annotation by keeping the proxy DaemonSet while still cleaning LBPort and DNSRecord objects.
- Kept invalid, conflicting, and Local-without-ready-endpoints failure paths fail-closed by forcing stale proxy DaemonSet cleanup even when keep-on-uninstall is set.
- Added controller tests for DaemonSet annotation rendering, Service deletion, type changes, and forced cleanup for invalid Services.
- Updated README.md and CLAUDE.md to document the implemented behavior and remove keep-on-uninstall from the remaining pre-Helm backlog.

**Explicit user constraints:**
- Keep Cloudflare and DNS-provider integrations out of scope.

---

### Record core Endpoints decision [12:51:59] <ez7pzjo8uf>

🟢 OpenAI Codex (`gpt-5.5`) [chore/contract-tightening]

**Tags:** #docs #contract-tightening #endpoints

**User request summary:** Record the decision that Local endpoint routing should keep using core Endpoints and should not introduce EndpointSlices.

**Implementation summary:**
- Updated CLAUDE.md to treat core Endpoints as the intentional Local routing API and explicitly say not to introduce EndpointSlices for this small-cluster design.
- Updated README.md to remove Endpoint API choice from the remaining pre-Helm backlog and document that core Endpoints are intentional.

**Explicit user constraints:**
- Do not introduce EndpointSlices.