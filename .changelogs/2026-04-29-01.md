### Implement Local endpoint proxy routing [12:03:28] <udeua6i7tf>

🟢 OpenAI Codex (`gpt-5.5`) [phase4/node-ip-labeler]

**Tags:** #external-traffic-policy #controller #proxy

**User request summary:** Finish the in-progress externalTrafficPolicy Local implementation so the controller passes endpoint information to the proxy and the proxy dials endpoints instead of ClusterIP.

**Implementation summary:**
- Added controller endpoint discovery for Services annotated with bulb.toturi.tech/external-traffic-policy=Local and rendered endpoint hostports into proxy DaemonSet args.
- Added proxy endpoint parsing and round-robin endpoint upstream selection for TCP and UDP forwarding specs.
- Added Endpoints watch and RBAC plus controller/proxy tests covering ready endpoint propagation and endpoint argument parsing.

---

### Refresh contract-tightening project docs [12:18:23] <mdto8kyiuf>

🟢 OpenAI Codex (`gpt-5.5`) [chore/contract-tightening]

**Tags:** #docs #contract-tightening

**User request summary:** Create a feature branch for the whole contract-tightening scope, then refresh CLAUDE.md and README.md with current project knowledge, explicit remaining work, and removal of stale statements.

**Implementation summary:**
- Created the chore/contract-tightening branch from main.
- Updated CLAUDE.md to reflect the current v0.0.5 Phase 4 baseline, current components, deferred DNS provider work, and the pre-Helm contract-tightening backlog.
- Updated README.md to remove stale Phase 1/static ConfigMap instructions and document the current manifest flow, dry-run DNS behavior, remaining contract work, and deferred packaging.

---

### Refresh README manifest requirements [12:23:14] <e5iiz7nmuf>

🟢 OpenAI Codex (`gpt-5.5`) [chore/contract-tightening]

**Tags:** #docs #contract-tightening

**User request summary:** Make README.md more current by removing Phase 1 sanity-check wording and documenting supported firewall backends beyond RHEL.

**Implementation summary:**
- Reframed the manifest section as the current post-Phase-4 raw manifest baseline rather than a Phase 1-style trial flow.
- Updated host requirements to require Linux nodes with firewalld, iptables, or nftables instead of a RHEL-family firewalld-only setup.

**Explicit user constraints:**
- Keep Cloudflare and DNS-provider integration work out of scope.

---

### Clarify README phase status [12:25:28] <y6yh2rdouf>

🟢 OpenAI Codex (`gpt-5.5`) [chore/contract-tightening]

**Tags:** #docs #contract-tightening

**User request summary:** Reread README.md and make it actually up to date, especially around deferred DNS integrations and completedness of the project phases.

**Implementation summary:**
- Made the README status block explicitly state which phases are done, mostly done, or not started.
- Replaced the vague roadmap phase wording with a phase completeness table and concrete remaining work per phase.
- Clarified that DNS provider publishing is intentionally deferred without naming Cloudflare as part of the active scope.

**Explicit user constraints:**
- Ignore Cloudflare and related DNS provider integration work as an implementation scope.