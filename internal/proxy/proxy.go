// Package proxy implements the bulb dataplane: an L4 splice that
// accepts on hostPort and forwards to the Service ClusterIP.
package proxy

import "errors"

func Run(args []string) error {
	return errors.New("not yet implemented (Phase 1)")
}
