# DEPRECATED: This ConfigMap is no longer used. Per-node public IPs are
# now discovered automatically by the node-ip-labeler DaemonSet and
# stored as Node annotations (bulb.toturi.tech/public-ipv4,
# bulb.toturi.tech/public-ipv6). You can safely delete this file.
apiVersion: v1
kind: ConfigMap
metadata:
  name: node-ips
  namespace: bulb-system
  labels:
    app.kubernetes.io/name: bulb
    app.kubernetes.io/component: controller
data:
  node-a: 203.0.113.10
  node-b: 203.0.113.11
  node-c: 203.0.113.12
